<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lsh.generator.dao.UserDao">

    <resultMap type="com.lsh.generator.entity.User" id="UserMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="name" column="name" jdbcType="VARCHAR"/>
        <result property="phone" column="phone" jdbcType="VARCHAR"/>
        <result property="password" column="password" jdbcType="VARCHAR"/>
        <result property="birthday" column="birthday" jdbcType="VARCHAR"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
        <result property="updateTime" column="update_time" jdbcType="TIMESTAMP"/>
        <result property="deleteTime" column="delete_time" jdbcType="TIMESTAMP"/>
        <result property="flag" column="flag" jdbcType="INTEGER"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="UserMap">
        select
          id, name, phone, password, birthday, create_time, update_time, delete_time, flag
        from day27.user
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="UserMap">
        select
          id, name, phone, password, birthday, create_time, update_time, delete_time, flag
        from day27.user
        limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="UserMap">
        select
        id, name, phone, password, birthday, create_time, update_time, delete_time, flag
        from day27.user
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="name != null and name != ''">
                and name = #{name}
            </if>
            <if test="phone != null and phone != ''">
                and phone = #{phone}
            </if>
            <if test="password != null and password != ''">
                and password = #{password}
            </if>
            <if test="birthday != null">
                and birthday = #{birthday}
            </if>
            <if test="createTime != null">
                and create_time = #{createTime}
            </if>
            <if test="updateTime != null">
                and update_time = #{updateTime}
            </if>
            <if test="deleteTime != null">
                and delete_time = #{deleteTime}
            </if>
            <if test="flag != null">
                and flag = #{flag}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into day27.user(name, phone, password, birthday, create_time, update_time, delete_time, flag)
        values (#{name}, #{phone}, #{password}, #{birthday}, #{createTime}, #{updateTime}, #{deleteTime}, #{flag})
    </insert>

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into day27.user(name, phone, password, birthday, create_time, update_time, delete_time, flag)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.name}, #{entity.phone}, #{entity.password}, #{entity.birthday}, #{entity.createTime},
            #{entity.updateTime}, #{entity.deleteTime}, #{entity.flag})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into day27.user(name, phone, password, birthday, create_time, update_time, delete_time, flag)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.name}, #{entity.phone}, #{entity.password}, #{entity.birthday}, #{entity.createTime},
            #{entity.updateTime}, #{entity.deleteTime}, #{entity.flag})
        </foreach>
        on duplicate key update
        name = values(name) , phone = values(phone) , password = values(password) , birthday = values(birthday) ,
        create_time = values(create_time) , update_time = values(update_time) , delete_time = values(delete_time) , flag
        = values(flag)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update day27.user
        <set>
            <if test="name != null and name != ''">
                name = #{name},
            </if>
            <if test="phone != null and phone != ''">
                phone = #{phone},
            </if>
            <if test="password != null and password != ''">
                password = #{password},
            </if>
            <if test="birthday != null">
                birthday = #{birthday},
            </if>
            <if test="createTime != null">
                create_time = #{createTime},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
            <if test="deleteTime != null">
                delete_time = #{deleteTime},
            </if>
            <if test="flag != null">
                flag = #{flag},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from day27.user where id = #{id}
    </delete>

</mapper>

